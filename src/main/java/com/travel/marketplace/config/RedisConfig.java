package com.travel.marketplace.config;

// RedisConfig has been removed.
// Redis was required in Phase 1 but is no longer needed.
// Refresh token management is handled by ConcurrentHashMap in AuthService.
// Redis will be re-introduced as an optional module in Phase 5 (Caching & Performance).
